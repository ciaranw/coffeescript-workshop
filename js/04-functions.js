// Generated by CoffeeScript 1.3.3
(function() {
  var debug, i, myFunction, myObj, printName, wait, _i,
    __slice = [].slice;

  myFunction = function() {
    return console.log('hello');
  };

  myFunction = function() {
    return console.log('hi');
  };

  printName = function(first, last) {
    return console.log('name is: ' + first + ' ' + last);
  };

  debug = function() {
    var arg, args, message, _i, _len, _results;
    message = arguments[0], args = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
    _results = [];
    for (_i = 0, _len = args.length; _i < _len; _i++) {
      arg = args[_i];
      _results.push(console.log(message, arg));
    }
    return _results;
  };

  debug('my message', 42, {
    key: 'value'
  });

  myFunction();

  myObj = {
    alert: function(text) {
      return console.log(text);
    },
    onClick: function(event) {
      return this.alert(event.name);
    },
    attach: function() {
      return $('.button').click(function(e) {
        return this.onClick(e);
      });
    }
  };

  wait = function(fn) {
    return setTimeout(fn, 1000);
  };

  for (i = _i = 1; _i <= 5; i = ++_i) {
    wait(function() {
      return console.log(i);
    });
  }

}).call(this);
